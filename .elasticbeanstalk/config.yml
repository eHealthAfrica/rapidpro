branch-defaults:
  default:
    environment: rapidproMaps-stage
  feature/nathan/dockerize:
    environment: rapidproMaps-stage
  master:
    environment: rapidproMaps-stage
global:
  application_name: rapidpro-maps
  default_ec2_keyname: aws-deploy
  default_platform: 64bit Amazon Linux 2015.09 v2.0.6 running Multi-container Docker
    1.7.1 (Generic)
  default_region: eu-central-1
  profile: null
  sc: git
deploy:
    artifact: conf/deploy.zip
